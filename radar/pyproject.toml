[project]
name = "weathercast-radar"
version = "0.1.0"
description = "Strict NOAA MRMS decoding for Weathercast"
requires-python = ">=3.12,<3.15"
dependencies = [
  "eccodes>=2.47.0,<3",
  "eccodeslib==2.47.3.22",
]

[dependency-groups]
dev = []

[tool.uv]
package = false
